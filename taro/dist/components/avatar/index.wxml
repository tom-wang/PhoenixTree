<template name="Avatar">
    <block>
        <view class="avatar" bindtap="Avatar__onClick" data-component-path="{{$path}}"
        data-component-class="Avatar" data-event-onClick-scope="this">
            <block wx:if="{{isValidSrc}}">
                <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}"></view>
            </block>
        </view>
    </block>
</template>